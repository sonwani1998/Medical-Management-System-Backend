services:
  - name: medical-management-system
    type: web
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: production

buildCommand: ./mvnw clean install -DskipTests
startCommand: java -jar target/your-spring-app.jar

